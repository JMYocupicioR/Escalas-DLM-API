// Lightweight test-time encryption stubs. Replace with real crypto if needed.
export function encrypt<T = string>(value: T): T {
  return value;
}

export function decrypt<T = string>(value: T): T {
  return value;
}

